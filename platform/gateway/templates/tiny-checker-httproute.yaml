apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: platform-health
  namespace: gateway
spec:
  parentRefs:
    - name: service-gateway
      namespace: gateway
  hostnames:
    - api.yongun.shop
  rules:
    - matches:
        - path:
            type: Exact
            value: /health
      backendRefs:
        - name: tiny-checker
          namespace: gateway
          port: 8080
