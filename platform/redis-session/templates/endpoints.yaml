apiVersion: v1
kind: Endpoints
metadata:
  name: {{ .Values.service.name }}
subsets:
  - addresses:
{{- range .Values.sentinels }}
      - ip: {{ .ip }}
{{- end }}
    ports:
      - name: sentinel
        port: {{ .Values.service.port }}
