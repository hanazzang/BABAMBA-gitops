apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

configMapGenerator:
  - name: k6-scenario-config
    files:
      - photo-load-test.js

generatorOptions:
  disableNameHash: true

namespace: k6