envoygateway-wrapper:
  envoy-gateway:
    deployment:
      replicas: 1 # 개발 환경이라 1개만
    service:
      type: ClusterIP
      # 온프레미스 MetalLB 등을 사용한다면 아래와 같이 IP 지정 가능
      # annotations: 
      #   metallb.universe.tf/loadBalancerIPs: 192.168.1.100