apiVersion: v1
kind: Service
metadata:
  name: {{ include "employee-server.fullname" . }}-get-stable
  annotations:
    argocd.argoproj.io/sync-wave: "0"
  labels:
    {{- include "employee-server.labels" . | nindent 4 }}
spec:
  ports:
    - name: http
      protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
  selector:
    {{- include "employee-server.selectorLabels.get" . | nindent 4 }}

---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "employee-server.fullname" . }}-get-canary
  annotations:
    argocd.argoproj.io/sync-wave: "0"
  labels:
    {{- include "employee-server.labels" . | nindent 4 }}
spec:
  ports:
    - name: http
      protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
  selector:
    {{- include "employee-server.selectorLabels.get" . | nindent 4 }}