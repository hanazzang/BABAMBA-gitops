apiVersion: k6.io/v1alpha1
kind: K6
metadata:
  name: photo-test
  namespace: k6
spec:
  parallelism: 2
  script:
    configMap:
      name: k6-scenario-config # kustomization에서 만든 이름
      file: photo-load-test.js