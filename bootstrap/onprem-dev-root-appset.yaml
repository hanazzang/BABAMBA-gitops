apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: onprem-dev-root-appset
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/hanazzang/BABAMBA-gitops
    targetRevision: argos-cloud
    path: .
    directory:
      recurse: true
      include: '{applicationsets/onprem-dev/*.yaml}'
  destination:
    server: https://kubernetes.default.svc
    namespace: argocd
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
